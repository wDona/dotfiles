# Metricas centradas en frametimes y 1% low
fps
fps_metrics=avg,0.01,0.001
frametime
frame_timing=1

# CPU / GPU: para ver quien es el cuello de botella en las caidas
cpu_stats
cpu_temp
cpu_mhz
core_load
gpu_stats
gpu_temp
gpu_core_clock
gpu_mem_clock
vram
ram

# Contexto
engine_version
vulkan_driver
resolution

position=top-left
font_size=20
background_alpha=0.4
toggle_hud=Shift_R+F12
